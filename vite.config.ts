import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // The development server is no longer the primary way to run the app,
  // but this config is still used by `npm run build`.
  // The proxy has been removed as it's not needed for the single-server setup.
})
